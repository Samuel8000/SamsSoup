@model IEnumerable<IdentityRole>

    <h2>Role Management</h2>

@if (!Model.Any())
{
    <tr><td colspan="3" class="text-center">No roles defined yet</td></tr>
}
else
{
    <table class="table">
        <tr>
            <th>Role ID</th>
            <th>Role Name</th>
            <th>#Users</th>
        </tr>
        @foreach(var role in Model)
        {
            <tr>
                <td>@role.Id</td>
                <td>@role.Name</td>
                <td>
                    <form asp-action="DeleteRole" asp-route-id="@role.Id" method="post">
                        <a class="btn btn-sm btn-primary" asp-action="EditRole" asp-route-id="@role.Id">Edit</a>
                        <button type="submit" class="btn btn-sm btn-danger" onclick="return confirm('Do you really want to delete this role?');">
                            Delete
                        </button>
                    </form>
                </td>
            </tr>
        }
    </table>
}

<a asp-action="AddNewRole" asp-controller="Admin" class="btn btn-primary">Add New Role</a>